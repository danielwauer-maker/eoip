name: EOIP CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: eoip_test
          POSTGRES_USER: eoip
          POSTGRES_PASSWORD: eoip_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U eoip -d eoip_test"
          --health-interval 3s
          --health-timeout 3s
          --health-retries 20

    env:
      EOIP_TEST_DATABASE_URL: postgresql://eoip:eoip_test@localhost:5432/eoip_test

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -e ".[dev]"
      - run: pytest
